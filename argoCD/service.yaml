apiVersion: v1
kind: Service
metadata:
  name: rielu-gen
  namespace: rielu-gen
spec:
  type: ClusterIP  # ここ（selectorと同じ階層）に配置
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: rielu-gen
